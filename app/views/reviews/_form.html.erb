<div class="form-control">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :restaurant%>
    <%= f.input :content %>
    <%= f.submit "Review", class: "btn btn-success" %>
  <% end %>
</div>
